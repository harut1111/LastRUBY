<div class="container">
	<div class="row">
		<div class="col">
			<%= form_for [:admin, @user] do |f| %>
			<%= f.select :status, options_for_select(User:: 
			STATUSES,{class: 'form-select'}) %>
			<div class="form-group">
				<%= f.submit class: 'btn btn-primary' %>
			</div>
			<% end %>
		</div>
	</div>
</div>